---
- name: first playbook
  hosts: all
  become: true
  tasks:
  
  - name: update ubuntu
    apt:
      update_cache: yes
      upgrade: dist
    when: ansible_distribution == "Ubuntu"

  - name: update centos
    yum:
      name: "*"
      state: latest
    when: ansible_distribution == "CentOS"
      
